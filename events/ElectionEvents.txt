###########################
# Election Events
###########################

add_namespace = election
add_namespace = FRA_election_events

# Test Election Event
country_event = {
	id = election.1
	title = election.1.t
	desc = election.1.d
	picture = GFX_report_event_election_vote
	
	is_triggered_only = yes
	
	option = {
		name = election.1.a
	}
}

country_event = { #Election concluded, info event FRANCE
	id = FRA_election_events.1

	title = {
		text = FRA_election_events.1.t.a
		trigger = {
			check_variable = {
				var = FRA_ruling_party
				value = 0
				compare = equals
			}
		}
	}
	title = {
		text = FRA_election_events.1.t.b
		trigger = {
			check_variable = {
				var = FRA_ruling_party
				value = 1
				compare = equals
			}
		}
	}

	desc = {
		text = FRA_election_events.1.desc.a
		trigger = {
			check_variable = {
				var = FRA_ruling_party
				value = 0
				compare = equals
			}
		}
	}
	desc = {
		text = FRA_election_events.1.desc.b
		trigger = {
			check_variable = {
				var = FRA_ruling_party
				value = 1
				compare = equals
			}
		}
	}

	picture = GFX_report_event_ast_parliament_house

	is_triggered_only = yes
	
	option = {
		name = FRA_election_events.1.a

		# hidden_effect = {
		# 	AST_democratic_votes_decrease_5_effect = yes
		# }

		IF = {
			limit = {
				check_variable = {
					var = FRA_ruling_party
					value = 0 #UAP
					compare = equals
				}
			}
			custom_effect_tooltip = FRA_left_election_win
		}

		IF = {
			limit = {
				check_variable = {
					var = FRA_ruling_party
					value = 1 #ALP
					compare = equals
				}
			}
			custom_effect_tooltip = FRA_right_election_win
		}
	}
}

# Wartime Exception
country_event = {
	id = election.14
	title = election.14.t
	desc = election.14.d
	picture = GFX_report_event_tank_factory
	
	is_triggered_only = yes

	trigger = {
		NOT = { 
			AND = { 
				tag = USA 
				date < 1949.1.1
			} 
		}
		OR = { NOT = { tag = SPR } NOT = { date < 1937.1.1 } }
		has_government = democratic
		has_war = yes
	}
	
	option = {
		name = election.14.a
		
			trigger = {
		NOT = { 
			AND = { 
				tag = USA 
				date < 1949.1.1
			} 
		}
		OR = { NOT = { tag = SPR } NOT = { date < 1937.1.1 } }
		has_government = democratic
		has_war = yes
		NOT = { has_country_flag = no_more_we }
	}
			set_country_flag = 	no_more_we
		add_stability = -0.05
	}

	option = {
		name = election.14.b
		
			trigger = {
		NOT = { 
			AND = { 
				tag = USA 
				date < 1949.1.1
			} 
		}
		OR = { NOT = { tag = SPR } NOT = { date < 1937.1.1 } }
		has_government = democratic
		has_war = yes
		NOT = { has_country_flag = no_more_we }
	}
	set_country_flag = 	no_more_we
		add_political_power = 20
		add_stability = 0.05
		add_popularity = {
			ideology = communism
			popularity = 0.05
		}
		add_popularity = {
			ideology = fascism
			popularity = 0.05
		}
	}
}

# Government Contested
country_event = {
	id = election.15
	title = election.15.t
	desc = election.15.d
	picture = GFX_report_event_gathering_protest
	
	is_triggered_only = yes

	trigger = {
		NOT = { 
			AND = { 
				tag = USA 
				date < 1949.1.1
			} 
		}
		OR = { NOT = { tag = SPR } NOT = { date < 1937.1.1 } }
		has_government = democratic
		has_war = yes
		any_war_score < 20
	}
	
	option = {
		name = election.15.a
		
		trigger = {
		NOT = { 
			AND = { 
				tag = USA 
				date < 1949.1.1
			} 
		}
		OR = { NOT = { tag = SPR } NOT = { date < 1937.1.1 } }
		has_government = democratic
		has_war = yes
		any_war_score < 20
		NOT = { has_country_flag = no_more_gc }
	}
			set_country_flag = no_more_gc

		add_political_power = -50
		add_stability = -0.05
	}

	option = {
		name = election.15.b
	trigger = {
		NOT = { 
			AND = { 
				tag = USA 
				date < 1949.1.1
			} 
		}
		OR = { NOT = { tag = SPR } NOT = { date < 1937.1.1 } }
		has_government = democratic
		has_war = yes
		any_war_score < 20
		NOT = { has_country_flag = no_more_gc }
	}
	set_country_flag = no_more_gc
		add_political_power = 25
		add_stability = 0.05
		add_popularity = {
			ideology = communism
			popularity = 0.07
		}
		add_popularity = {
			ideology = fascism
			popularity = 0.07
		}
	}
}
